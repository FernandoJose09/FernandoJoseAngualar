import { environment } from './../../environments/environment';

export const auth = {
  login: `${environment.urlBase}SignIn`,

}

{
export const auth = {
  registro: `${environment.urlBase}InsertRegistro`,
}
